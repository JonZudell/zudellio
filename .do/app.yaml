name: zudellio
static_sites:
  - name: interface
    github:
      repo: JonZudell/zudellio
      branch: main
      source_dir: interface
    build_command: npm run build
    output_dir: public
    routes:
      - path: /
